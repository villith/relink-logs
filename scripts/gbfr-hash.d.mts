/** Types for gbfr-hash.mjs, so TypeScript tests can import the hash directly. */

/** The game's custom XXHash32, as eight lowercase hex digits. */
export declare const gameXxhash32: (text: string) => string;
